/**
 * routes/index.js
 *
 * The index route is the main home route of the application.
 */
import Ember from 'ember';
import Base from 'portfolio-dx16/routes/base';

export default Base.extend({
});
